var gameState = require('app/gameState');

function handleUserClick(event) {
  console.log('click w TD');
  gameState.placePlayer(event.data.posX, event.data.posY);
}

module.exports = handleUserClick;
